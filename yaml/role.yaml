apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: falcon-sensor-scc-privileged-role 
  namespace: falcon-sensor 
rules:
- apiGroups:
  - security.openshift.io 
  resourceNames:
  - privileged
  resources:
  - securitycontextconstraints 
  verbs: 
  - use